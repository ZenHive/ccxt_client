defmodule CCXT.Types.Schema.Option do
  @moduledoc """
  Auto-generated schema for `CCXT.Types.Option`.

  Source: priv/ccxt/ts/src/base/types.ts

  ## Fields

  - `raw` - any()
  - `currency` - String.t()
  - `symbol` - String.t()
  - `timestamp` - number() (optional)
  - `datetime` - String.t() | nil (optional)
  - `implied_volatility` - number()
  - `open_interest` - number()
  - `bid_price` - number()
  - `ask_price` - number()
  - `mid_price` - number()
  - `mark_price` - number()
  - `last_price` - number()
  - `underlying_price` - number()
  - `change` - number()
  - `percentage` - number()
  - `base_volume` - number()
  - `quote_volume` - number()

  ## Usage

  This module is used by `CCXT.Types.Option`:

      defmodule CCXT.Types.Option do
        use CCXT.Types.Schema.Option
      end

  Do not edit this file manually - it will be overwritten by `mix ccxt.gen.types`.

  """

  @fields [
    %{name: :raw, optional: false, source: "info", type: "any()"},
    %{name: :currency, optional: false, source: "currency", type: "String.t()"},
    %{name: :symbol, optional: false, source: "symbol", type: "String.t()"},
    %{name: :timestamp, optional: true, source: "timestamp", type: "number()"},
    %{name: :datetime, optional: true, source: "datetime", type: "String.t() | nil"},
    %{name: :implied_volatility, optional: false, source: "impliedVolatility", type: "number()"},
    %{name: :open_interest, optional: false, source: "openInterest", type: "number()"},
    %{name: :bid_price, optional: false, source: "bidPrice", type: "number()"},
    %{name: :ask_price, optional: false, source: "askPrice", type: "number()"},
    %{name: :mid_price, optional: false, source: "midPrice", type: "number()"},
    %{name: :mark_price, optional: false, source: "markPrice", type: "number()"},
    %{name: :last_price, optional: false, source: "lastPrice", type: "number()"},
    %{name: :underlying_price, optional: false, source: "underlyingPrice", type: "number()"},
    %{name: :change, optional: false, source: "change", type: "number()"},
    %{name: :percentage, optional: false, source: "percentage", type: "number()"},
    %{name: :base_volume, optional: false, source: "baseVolume", type: "number()"},
    %{name: :quote_volume, optional: false, source: "quoteVolume", type: "number()"}
  ]
  @type_spec_str """
  %__MODULE__{
    raw: any(),
    currency: String.t(),
    symbol: String.t(),
    timestamp: number() | nil,
    datetime: String.t() | nil,
    implied_volatility: number(),
    open_interest: number(),
    bid_price: number(),
    ask_price: number(),
    mid_price: number(),
    mark_price: number(),
    last_price: number(),
    underlying_price: number(),
    change: number(),
    percentage: number(),
    base_volume: number(),
    quote_volume: number()
  }
  """
  @type_spec Code.string_to_quoted!(@type_spec_str)

  defmacro __using__(opts) do
    fields = @fields
    type_spec = @type_spec
    struct_defaults = Keyword.get(opts, :struct_defaults, [])
    field_names = Enum.map(fields, & &1.name)

    struct_fields =
      Enum.map(field_names, fn name ->
        if Keyword.has_key?(struct_defaults, name) do
          {name, Keyword.fetch!(struct_defaults, name)}
        else
          name
        end
      end)

    quote do
      import CCXT.Types.Helpers, only: [get_value: 2, get_camel_value: 3]

      @fields unquote(Macro.escape(fields))
      @type_spec unquote(Macro.escape(type_spec))
      @type t :: unquote(type_spec)
      defstruct unquote(struct_fields)
      @spec from_map(map()) :: t()
      def from_map(map) when is_map(map) do
        %__MODULE__{
          raw: get_camel_value(map, :raw, :info),
          currency: get_value(map, :currency),
          symbol: get_value(map, :symbol),
          timestamp: get_value(map, :timestamp),
          datetime: get_value(map, :datetime),
          implied_volatility: get_camel_value(map, :implied_volatility, :impliedVolatility),
          open_interest: get_camel_value(map, :open_interest, :openInterest),
          bid_price: get_camel_value(map, :bid_price, :bidPrice),
          ask_price: get_camel_value(map, :ask_price, :askPrice),
          mid_price: get_camel_value(map, :mid_price, :midPrice),
          mark_price: get_camel_value(map, :mark_price, :markPrice),
          last_price: get_camel_value(map, :last_price, :lastPrice),
          underlying_price: get_camel_value(map, :underlying_price, :underlyingPrice),
          change: get_value(map, :change),
          percentage: get_value(map, :percentage),
          base_volume: get_camel_value(map, :base_volume, :baseVolume),
          quote_volume: get_camel_value(map, :quote_volume, :quoteVolume)
        }
      end

      defoverridable from_map: 1
    end
  end
end

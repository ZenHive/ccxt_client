defmodule CCXT.Types.Schema.BalanceEntry do
  @moduledoc """
  Auto-generated schema for `CCXT.Types.BalanceEntry`.

  Source: priv/ccxt/ts/src/base/types.ts

  ## Fields

  - `free` - number() | nil
  - `used` - number() | nil
  - `total` - number() | nil
  - `debt` - number() | nil (optional)

  ## Usage

  This module is used by `CCXT.Types.BalanceEntry`:

      defmodule CCXT.Types.BalanceEntry do
        use CCXT.Types.Schema.BalanceEntry
      end

  Do not edit this file manually - it will be overwritten by `mix ccxt.gen.types`.

  """

  @fields [
    %{name: :free, optional: false, source: "free", type: "number() | nil"},
    %{name: :used, optional: false, source: "used", type: "number() | nil"},
    %{name: :total, optional: false, source: "total", type: "number() | nil"},
    %{name: :debt, optional: true, source: "debt", type: "number() | nil"}
  ]
  @type_spec_str """
  %__MODULE__{free: number() | nil, used: number() | nil, total: number() | nil, debt: number() | nil}
  """
  @type_spec Code.string_to_quoted!(@type_spec_str)

  defmacro __using__(opts) do
    fields = @fields
    type_spec = @type_spec
    struct_defaults = Keyword.get(opts, :struct_defaults, [])
    field_names = Enum.map(fields, & &1.name)

    struct_fields =
      Enum.map(field_names, fn name ->
        if Keyword.has_key?(struct_defaults, name) do
          {name, Keyword.fetch!(struct_defaults, name)}
        else
          name
        end
      end)

    quote do
      import CCXT.Types.Helpers, only: [get_value: 2, get_camel_value: 3]

      @fields unquote(Macro.escape(fields))
      @type_spec unquote(Macro.escape(type_spec))
      @type t :: unquote(type_spec)
      defstruct unquote(struct_fields)
      @spec from_map(map()) :: t()
      def from_map(map) when is_map(map) do
        %__MODULE__{
          free: get_value(map, :free),
          used: get_value(map, :used),
          total: get_value(map, :total),
          debt: get_value(map, :debt)
        }
      end

      defoverridable from_map: 1
    end
  end
end

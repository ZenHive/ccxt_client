defmodule CCXT.Types.Schema.MarketMarginModes do
  @moduledoc """
  Auto-generated schema for `CCXT.Types.MarketMarginModes`.

  Source: priv/ccxt/ts/src/base/types.ts

  ## Fields

  - `isolated` - boolean()
  - `cross` - boolean()

  ## Usage

  This module is used by `CCXT.Types.MarketMarginModes`:

      defmodule CCXT.Types.MarketMarginModes do
        use CCXT.Types.Schema.MarketMarginModes
      end

  Do not edit this file manually - it will be overwritten by `mix ccxt.gen.types`.

  """

  @fields [
    %{name: :isolated, optional: false, source: "isolated", type: "boolean()"},
    %{name: :cross, optional: false, source: "cross", type: "boolean()"}
  ]
  @type_spec_str """
  %__MODULE__{isolated: boolean(), cross: boolean()}
  """
  @type_spec Code.string_to_quoted!(@type_spec_str)

  defmacro __using__(opts) do
    fields = @fields
    type_spec = @type_spec
    struct_defaults = Keyword.get(opts, :struct_defaults, [])
    field_names = Enum.map(fields, & &1.name)

    struct_fields =
      Enum.map(field_names, fn name ->
        if Keyword.has_key?(struct_defaults, name) do
          {name, Keyword.fetch!(struct_defaults, name)}
        else
          name
        end
      end)

    quote do
      import CCXT.Types.Helpers, only: [get_value: 2, get_camel_value: 3]

      @fields unquote(Macro.escape(fields))
      @type_spec unquote(Macro.escape(type_spec))
      @type t :: unquote(type_spec)
      defstruct unquote(struct_fields)
      @spec from_map(map()) :: t()
      def from_map(map) when is_map(map) do
        %__MODULE__{
          isolated: get_value(map, :isolated),
          cross: get_value(map, :cross)
        }
      end

      defoverridable from_map: 1
    end
  end
end
